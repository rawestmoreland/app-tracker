{
  "$schema": "https://turborepo.com/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "tasks": {
    "build": {
      "dependsOn": ["^build", "^db:generate:prod", "^db:migrate:deploy"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"],
      "env": [
        "DATABASE_URL",
        "NODE_ENV",
        "NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY",
        "CLERK_SECRET_KEY",
        "CLERK_CALLBACK_URL",
        "NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL",
        "NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL",
        "NEXT_PUBLIC_CLERK_SIGN_IN_URL",
        "NEXT_PUBLIC_CLERK_SIGN_UP_URL",
        "CLERK_WEBHOOK_SIGNING_SECRET",
        "R2_ENDPOINT",
        "R2_ACCESS_KEY_ID",
        "R2_SECRET_ACCESS_KEY",
        "R2_BUCKET_NAME",
        "CLERK_USERNAME",
        "CLERK_PASSWORD"
      ]
    },
    "db:migrate:dev": {
      "cache": false,
      "persistent": true
    },
    "db:migrate": {
      "cache": false
    },
    "db:migrate:deploy": {
      "cache": false,
      "env": ["DATABASE_URL"]
    },
    "db:push": {
      "cache": false
    },
    "db:seed": {
      "cache": false
    },
    "dev": {
      "dependsOn": ["^db:generate:dev"],
      "cache": false,
      "persistent": true
    },
    "db:generate:dev": {
      "cache": false
    },
    "db:generate:prod": {
      "cache": false
    },
    "deploy": {
      "dependsOn": ["^deploy", "^db:generate:prod", "^db:migrate:deploy"],
      "cache": false,
      "persistent": true,
      "env": ["DATABASE_URL"]
    },
    "test:e2e": {
      "inputs": ["cypress/**/*", "cypress.config.ts"],
      "env": ["CLERK_USERNAME", "CLERK_PASSWORD"]
    },
    "lint": {}
  }
}
